<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title></title>
  <title>Buy Premium PDFs for Study | PDF Store</title>
<meta name="description" content="Buy and download premium study PDFs, notes, and resources for students. Fast, secure, and affordable PDF downloads.">
<meta name="keywords" content="PDF, study, notes, download, buy, premium, education, resources, Bengaluru, India, students, academic, notes, ebooks, study materials, educational PDFs, premium PDFs, buy PDFs, download PDFs, study resources, academic resources, student notes, educational materials,engineering, bsc nursing ,sslc,neet, kcet ,jee,cbse,icse,ugc net,ugc net commerce,ugc net management,ugc net english">
<meta name="robots" content="index, follow">
<meta property="og:title" content="Buy Premium PDFs for Study | PDF Store">
<meta property="og:description" content="Buy and download premium study PDFs, notes, and resources for students.">
<meta property="og:image" content="https://pdfstore-qm6h.onrender.com/images/logo.png">
<meta property="og:url" content="https://pdfstore-qm6h.onrender.com/">
<meta name="twitter:card" content="summary_large_image">
<meta name="viewport" content="width=device-width, initial-scale=1">
<link rel="canonical" href="https://pdfstore-qm6h.onrender.com/">
  <link rel="icon" href="/images/logo.png" type="image/png" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet" />
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700;800&display=swap" rel="stylesheet">
  <script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "WebSite",
  "name": "PDF Store",
  "url": "https://pdfstore-qm6h.onrender.com/",
  "description": "Buy and download premium study PDFs, notes, and resources for students.",
  "publisher": {
    "@type": "Organization",
    "name": "PDF Store"
  }
}
</script>
  <style>
    body {
      font-family: 'Inter', Arial, sans-serif;
      min-height: 100vh;
      margin: 0;
      padding-bottom: 60px;
      /* Animated gradient background */
      background: linear-gradient(120deg, #ede9fe 0%, #f3f3f3 60%, #c7d2fe 100%);
      background-size: 200% 200%;
      animation: gradientMove 8s ease-in-out infinite;
      position: relative;
      overflow-x: hidden;
    }
    @keyframes gradientMove {
      0% { background-position: 0% 50%; }
      50% { background-position: 100% 50%; }
      100% { background-position: 0% 50%; }
    }
    /* Blurred SVG shape overlay for extra depth */
    .bg-shape {
      position: fixed;
      top: -120px;
      left: -120px;
      width: 600px;
      height: 600px;
      z-index: 0;
      pointer-events: none;
      opacity: 0.32;
      filter: blur(18px);
    }
    .bg-shape2 {
      position: fixed;
      bottom: -140px;
      right: -140px;
      width: 520px;
      height: 520px;
      z-index: 0;
      pointer-events: none;
      opacity: 0.22;
      filter: blur(24px);
    }
    .navbar {
      box-shadow: 0 2px 8px rgba(124,58,237,0.08);
    }
    .navbar-brand {
      font-weight: bold;
      letter-spacing: 1px;
      color: #7c3aed !important;
    }
    .main-title {
      text-align: center;
      padding: 38px 15px 34px;
      font-weight: 800;
      color: #7c3aed;
      font-size: 2.4rem;
      letter-spacing: 1.5px;
      background: linear-gradient(90deg, #ede9fe 0%, #fff 100%);
      border-radius: 18px;
      margin-bottom: 18px;
      box-shadow: 0 2px 12px rgba(124,58,237,0.06);
    }
    .card {
      border: none;
      border-radius: 22px;
      box-shadow: 0 8px 32px rgba(124,58,237,0.10), 0 1.5px 4px rgba(0,0,0,0.04);
      background: rgba(255,255,255,0.82);
      position: relative;
      margin-bottom: 32px;
      transition: box-shadow 0.18s, transform 0.18s;
      overflow: hidden;
    }
    .card:hover {
      box-shadow: 0 16px 48px rgba(124,58,237,0.16), 0 2px 8px rgba(0,0,0,0.08);
      transform: translateY(-4px) scale(1.012);
      border-color: #c7d2fe;
    }
    .card img {
      object-fit: cover;
      height: 180px;
      width: 100%;
      border-top-left-radius: 22px;
      border-top-right-radius: 22px;
      background: #f3f4f6;
      box-shadow: 0 2px 12px rgba(124,58,237,0.06);
    }
    .card-body {
      background: #fff;
      color: #222;
      border-bottom-left-radius: 18px;
      border-bottom-right-radius: 18px;
      padding: 18px 18px 10px 18px;
      min-height: 160px;
      display: flex;
      flex-direction: column;
      justify-content: flex-start;
    }
    .price {
      font-weight: 700;
      font-size: 1.25rem;
      color: #10b981;
      letter-spacing: 0.2px;
      background: #ecfdf5;
      padding: 2px 12px;
      border-radius: 8px;
      margin-right: 8px;
    }
    .card-title {
      font-size: 1.13rem;
      font-weight: 600;
      color: #3b2f7f;
      margin-bottom: 0.3rem;
      margin-top: 0.3rem;
      letter-spacing: 0.1px;
    }
    .card-text {
      color: #555;
      font-size: 1.01rem;
      margin-bottom: 0.5rem;
      flex-grow: 1;
    }
    .card-footer {
      background: rgba(248,250,252,0.92);
      border-top: none;
      border-bottom-left-radius: 22px;
      border-bottom-right-radius: 22px;
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 14px 22px;
      border-top: 1px solid #ececec;
    }
    .user-info {
      display: flex;
      align-items: center;
      gap: 10px;
      flex-wrap: nowrap;
    }
    .user-info img {
      border-radius: 50%;
      border: 2px solid #ede9fe;
      width: 32px;
      height: 32px;
      object-fit: cover;
      background: #f3f4f6;
      display: inline-block;
      max-width: 32px;
      max-height: 32px;
      min-width: 32px;
      min-height: 32px;
      flex-shrink: 0;
      flex-grow: 0;
    }
    .user-info .badge {
      margin-top: 0;
      vertical-align: middle;
      font-size: 0.98em;
      font-weight: 600;
      background: #f3f4f6;
      color: #7c3aed;
      border: 1px solid #ede9fe;
    }
    .btn-buy, .btn-success {
      background: linear-gradient(90deg, #7c3aed 0%, #6366f1 100%);
      color: #fff;
      font-weight: 700;
      border: none;
      border-radius: 10px;
      padding: 10px 28px;
      font-size: 1.08em;
      box-shadow: 0 2px 8px rgba(124,58,237,0.10);
      transition: background 0.18s, box-shadow 0.18s, transform 0.12s;
      cursor: pointer;
      display: flex;
      align-items: center;
      gap: 7px;
    }
    .btn-buy:hover, .btn-success:hover, .btn-buy:focus, .btn-success:focus {
      background: linear-gradient(90deg, #5b21b6 0%, #7c3aed 100%);
      color: #fff;
      box-shadow: 0 4px 16px rgba(124,58,237,0.18);
      transform: translateY(-2px) scale(1.03);
    }
    .btn-preview {
      background: #ede9fe;
      color: #7c3aed;
      font-weight: 600;
      border: none;
      border-radius: 8px;
      padding: 8px 18px;
      font-size: 1em;
      margin-right: 8px;
      transition: background 0.18s, color 0.18s, box-shadow 0.18s;
      box-shadow: 0 1px 4px rgba(124,58,237,0.07);
    }
    .btn-preview:hover {
      background: #7c3aed;
      color: #fff;
      box-shadow: 0 2px 8px rgba(124,58,237,0.13);
    }
    #toast {
      visibility: hidden;
      min-width: 200px;
      max-width: 320px;
      background-color: #4caf50;
      color: white;
      text-align: center;
      border-radius: 4px;
      padding: 16px 24px;
      position: fixed;
      z-index: 1055;
      left: 50%;
      bottom: 30px;
      font-size: 17px;
      transform: translateX(-50%);
      box-shadow: 0 4px 8px rgb(0 0 0 / 0.2);
      user-select: none;
      transition: background 0.2s;
    }
    .search-bar {
      margin: 32px auto 24px;
      max-width: 420px;
      display: flex;
      align-items: center;
      background: #fff;
      border-radius: 8px;
      box-shadow: 0 2px 8px rgba(124,58,237,0.07);
      padding: 6px 14px;
      
      border: 1px solid #ede9fe;
    }
    .search-bar input {
      border: none;
      outline: none;
      flex: 1;
      font-size: 1.1em;
      padding: 8px 0;
      background: transparent;
    }
    .search-bar .fa-search {
      color: #7c3aed;
      font-size: 1.2em;
      margin-right: 8px;
    }
    .btn-spinner {
      display: inline-block;
      width: 18px;
      height: 18px;
      border: 2.5px solid #fff;
      border-top: 2.5px solid #7c3aed;
      border-radius: 50%;
      animation: spin 0.7s linear infinite;
      vertical-align: middle;
      margin-left: 8px;
    }
    @keyframes spin {
      to { transform: rotate(360deg); }
    }
    .btn-buy.loading, .btn-success.loading {
      pointer-events: none;
      opacity: 0.7;
    }
    .category-scrollbar {
      display: flex;
      gap: 14px;
      overflow-x: auto;
      padding-bottom: 4px;
      scrollbar-width: none; /* Firefox */
      -ms-overflow-style: none; /* IE 10+ */
    }
    .category-scrollbar::-webkit-scrollbar {
      display: none; /* Chrome/Safari */
    }
    .category-btn {
      flex: 0 0 auto;
      min-width: 120px;
      max-width: 140px;
      text-align: center;
      font-size: 1.08em;
      padding: 10px 0;
      border-radius: 10px;
      background: #f3f4f6;
      color: #7c3aed;
      border: none;
      font-weight: 700;
      cursor: pointer;
      box-shadow: 0 1px 4px rgba(124,58,237,0.07);
      transition: background 0.18s, color 0.18s, box-shadow 0.18s;
    }
    .category-btn.active[data-category="all"] {
      background: linear-gradient(90deg, #ede9fe 0%, #c7d2fe 100%) !important;
      color: #7c3aed !important;
      box-shadow: 0 2px 8px rgba(124,58,237,0.10);
    }
  
    .category-btn.active {
      background: linear-gradient(90deg, #fbcfe8 0%, #a7f3d0 100%) !important;
      color: #7c3aed !important;
      box-shadow: 0 2px 8px rgba(16,185,129,0.13);
    }
    @media (max-width: 768px) {
      .main-title { font-size: 1.3rem; padding: 18px 8px 18px; }
      .card img { height: 120px; }
      .card-footer { flex-direction: column; gap: 10px; }
      .btn-buy, .btn-success { width: 100%; justify-content: center; }
      .user-info { 
        flex-direction: row !important; 
        align-items: center !important; 
        gap: 8px !important;
        justify-content: flex-start !important;
      }
    }
    @media (max-width: 576px) {
      .file-card {
        width: 100% !important;
        margin: 0 auto 18px auto !important;
        padding: 20px !important;
      }
      .card {
        border-radius: 14px !important;
        border: 1.5px solid #ececec !important;
        box-shadow: 0 2px 12px rgba(124,58,237,0.08) !important;
        background: #fff !important;
        margin-bottom: 18px !important;
        overflow: hidden !important;
        min-width: 0 !important;
      }
      .card img {
        border-radius: 14px 14px 0 0 !important;
        height: 140px !important;
        object-fit: cover !important;
        width: 100% !important;
        margin: 0 !important;
        box-shadow: none !important;
      }
      .card-body {
        padding: 14px 12px 8px 12px !important;
        min-height: unset !important;
        background: #fff !important;
      }
      .card-title {
        font-size: 1.08rem !important;
        font-weight: 700 !important;
        margin-bottom: 0.2rem !important;
        margin-top: 0.2rem !important;
        color: #222 !important;
        letter-spacing: 0.1px !important;
      }
      .card-text {
        font-size: 0.98rem !important;
        color: #555 !important;
        margin-bottom: 0.5rem !important;
      }
      .price {
        font-size: 1.13rem !important;
        padding: 2px 10px !important;
        border-radius: 7px !important;
        margin-right: 6px !important;
      }
      .user-info {
        gap: 7px !important;
      }
      .user-info img {
        width: 24px !important;
        height: 24px !important;
        min-width: 24px !important;
        min-height: 24px !important;
        max-width: 24px !important;
        max-height: 24px !important;
        border-width: 1.5px !important;
      }
      .user-info .badge {
        font-size: 0.92em !important;
        padding: 3px 10px !important;
        border-radius: 8px !important;
      }
      .card-footer {
        flex-direction: row !important;
        align-items: center !important;
        justify-content: space-between !important;
        padding: 10px 12px !important;
        border-radius: 0 0 14px 14px !important;
        border-top: 1px solid #ececec !important;
        background: #fafbfc !important;
        gap: 0 !important;
      }
      .btn-buy, .btn-success {
        width: auto !important;
        min-width: 110px !important;
        padding: 8px 16px !important;
        font-size: 1em !important;
        border-radius: 8px !important;
        box-shadow: 0 1px 4px rgba(124,58,237,0.07) !important;
      }
      .btn-preview {
        margin-right: 0 !important;
        margin-bottom: 6px !important;
        padding: 7px 14px !important;
        font-size: 0.98em !important;
        border-radius: 7px !important;
      }
      .search-bar {
        border: 2.5px solid #7c3aed !important;
        box-shadow: 0 2px 12px rgba(124,58,237,0.13) !important;
        background: #f8fafc !important;
        padding: 8px 16px !important;
        margin: 18px auto 18px !important;
        border-radius: 12px !important;
        transition: border 0.18s, box-shadow 0.18s;
      }
      .search-bar input {
        font-size: 1.13em !important;
        background: transparent !important;
      }
    }
    /* #notificationBox {
      background: rgba(255,255,255,0.95);
      box-shadow: 0 8px 32px rgba(124,58,237,0.18);
      border-radius: 16px;
      border: 1.5px solid #ede9fe;
      padding: 0;
      overflow: hidden;
      min-width: 340px;
    }
    #notificationBox > div:first-child {
      background: linear-gradient(90deg, #7c3aed 0%, #6366f1 100%);
      color: #fff;
      padding: 18px 24px;
      font-weight: 800;
      font-size: 1.18em;
      letter-spacing: 0.5px;
    } */
    footer {
      background: #f8fafc;
      border-top: 1.5px solid #ececec;
      padding: 32px 0 24px 0;
      margin-top: 64px;
      box-shadow: 0 -2px 8px rgba(124,58,237,0.04);
    }
    footer .container {
      font-size: 1.08em;
      color: #7c3aed;
      font-weight: 700;
    }
    body > *:not(.bg-shape):not(.bg-shape2):not([style*="pointer-events:none"]) {
      position: relative;
      z-index: 1;
    }
    body, .card-body, .main-title, .card-footer, footer {
  color: #222 !important;
}
  </style>
</head>
<body style="position:relative;">
  <!-- Glassmorphism overlay -->
  <div style="position:fixed;inset:0;z-index:0;pointer-events:none;">
    <div style="width:100vw;height:100vh;background:rgba(255,255,255,0.08);backdrop-filter:blur(3px);"></div>
  </div>

  <!-- Blurred SVG shapes for dynamic background -->
  <svg class="bg-shape" viewBox="0 0 600 600">
    <defs>
      <radialGradient id="grad1" cx="50%" cy="50%" r="80%">
        <stop offset="0%" stop-color="#a5b4fc"/>
        <stop offset="100%" stop-color="#ede9fe"/>
      </radialGradient>
    </defs>
    <ellipse cx="300" cy="300" rx="300" ry="220" fill="url(#grad1)" />
  </svg>
  <svg class="bg-shape2" viewBox="0 0 520 520">
    <defs>
      <radialGradient id="grad2" cx="50%" cy="50%" r="80%">
        <stop offset="0%" stop-color="#f0abfc"/>
        <stop offset="100%" stop-color="#c7d2fe"/>
      </radialGradient>
    </defs>
    <ellipse cx="260" cy="260" rx="260" ry="180" fill="url(#grad2)" />
  </svg>

  <!-- HEADER -->
<%-include('header')%>

  <div class="container" style="margin-top: 18px;">
    <div id="categoryFilter" style="display: flex; gap: 16px; justify-content: center; margin-bottom: 18px;">
      <div class="category-scrollbar">
        <button class="category-btn active" data-category="all">All</button>
        <% categories.forEach(category => { %>  
        <button class="category-btn" data-category="<%= category.toLowerCase() %>"><%= category %></button>
        <% }); %>
       
        <!-- Add more buttons as needed -->
      </div>
    </div>
  </div>
  <hr>

  <h2 class="main-title">Buy our <span style="color:#10b981;">Premium PDFs</span> for your studies</h2>

  <!-- Search Bar -->
  <div class="search-bar">
    <i class="fa fa-search"></i>
    <input type="text" id="searchInput" placeholder="Search by file name, description, or user..." />
  </div>

  <div class="container">
    <div class="row g-4" id="fileCards">
      <% files.forEach(file => { %>
      <div class="col-md-4 file-card"
           data-filename="<%= file.filename.toLowerCase() %>"
           data-description="<%= (file.filedescription || '').toLowerCase() %>"
           data-user="<%= (file.user || '').toLowerCase() %>"
           data-category="<%= (file.category || '').toLowerCase() %>">
        <div class="card shadow h-100">
          <img src="<%=file.previewUrl%>" alt="Preview of <%= file.filename %>" loading="lazy" />
          <div class="card-body d-flex flex-column">
            <div class="d-flex justify-content-between align-items-center mb-2">
              <div class="price" aria-label="Price">₹<%= file.price %></div>
             
              <button
                class="btn-preview"
                data-previewpdf="<%=file.previewUrl%>"
                title="Preview PDF"
              >
                <i class="fa fa-eye"></i> Preview
              </button>
            </div>
            <h5 class="card-title">FileName:&nbsp;<%= file.filename %></h5>
            <hr>
            <p class="card-text flex-grow-1"><%= file.filedescription %></p>
             <button
              class="btn btn-outline-secondary btn-sm btn-copy-link"
              data-link="/file/<%= file._id %>"
              title="Copy link to this file"
              style="margin-left:8px;display:flex;align-items:center;gap:5px;"
            >
              <i class="fa fa-link"></i> Copy Link
            </button>
          </div>
          <div class="card-footer d-flex justify-content-between align-items-center">
            <div class="user-info">
              <img src="/images/Screenshot 2025-04-18 194936.png" alt="User avatar" />
              <span> <% if (file.category) { %>
                <span class="badge rounded-pill"
                  style="background:#f3f4f6; color:#7c3aed; font-weight:600; font-size:0.98em; border:1px solid #ede9fe;">
                  <%= file.category %>
                </span>
              <% } %></span>
            </div>
            <% if (Number(file.price) > 0) { %>
              <button
                type="button"
                class="btn btn-buy btn-sm"
                id="buyBtn-<%= file._id %>"
                data-fileid="<%= file._id || file.id || '' %>"
                data-filename="<%= file.filename %>"
                data-price="<%= file.price %>"
                aria-label="Buy <%= file.filename %>"
              >
                <i class="fa fa-shopping-cart"></i> Buy Now
              </button>
            <% } else { %>
              <a
                href="/viewfile/<%= file._id %>"
                class="btn btn-success btn-sm"
                id="downloadBtn-<%= file._id %>"
                download
                aria-label="Download <%= file.filename %>.pdf"
                style="font-weight:700;display:flex;align-items:center;gap:7px;"
              >
                <i class="fa fa-download"></i> Download
              </a>
            <% } %>
           
          </div>
        </div>
      </div>
      <% }); %>
    </div>
  </div>

  <div id="toast" role="alert" aria-live="assertive" aria-atomic="true"></div>

  <!-- PDF Modal Preview -->
  <div class="modal fade" id="pdfModal" tabindex="-1" aria-labelledby="pdfModalLabel" aria-hidden="true">
  <div class="modal-dialog modal-dialog-centered modal-xl">
    <div class="modal-content" style="border-radius:18px;">
      <div class="modal-header">
        <h5 class="modal-title" id="pdfModalLabel">PDF Preview</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <div class="modal-body d-flex justify-content-center align-items-center" style="min-height:50vh;">
        <iframe id="pdfPreviewFrame" src="" style="width:100%;height:60vh;border:none;border-radius:12px;box-shadow:0 2px 12px rgba(0,0,0,0.08);"></iframe>
      </div>
    </div>
  </div>
</div>

  <!-- 2. Notification Modal/Box (place just after header) -->
  <div id="notificationBox" style="display:none;position:fixed;top:70px;right:30px;z-index:2000;min-width:320px;max-width:95vw;background:#fff;border-radius:12px;box-shadow:0 8px 32px rgba(0,0,0,0.18);padding:0 0 0 0;overflow:hidden;">
    <div style="background:#7c3aed;color:#fff;padding:16px 20px;font-weight:700;font-size:1.15em;">Notifications
      <button onclick="closeNotificationBox()" style="background:none;border:none;float:right;color:#fff;font-size:1.2em;cursor:pointer;">&times;</button>
    </div>
    <div id="notificationList" style="max-height:320px;overflow-y:auto;padding:12px 20px 16px 20px;">
      <div style="color:#888;">Loading...</div>
    </div>
  </div>

  <script src="https://checkout.razorpay.com/v1/checkout.js" defer></script>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" defer></script>
  <script defer>
    document.addEventListener("DOMContentLoaded", () => {
      const toast = document.getElementById("toast");
      const searchInput = document.getElementById("searchInput");
      const fileCards = document.querySelectorAll(".file-card");

      // Search/filter functionality
      searchInput.addEventListener("input", function () {
        const val = this.value.trim().toLowerCase();
        fileCards.forEach(card => {
          const filename = card.getAttribute("data-filename");
          const description = card.getAttribute("data-description");
          const user = card.getAttribute("data-user");
          if (
            filename.includes(val) ||
            description.includes(val) ||
            user.includes(val)
          ) {
            card.style.display = "";
          } else {
            card.style.display = "none";
          }
        });
      });

      function showToast(message, isError = false) {
        toast.textContent = message;
        toast.style.backgroundColor = isError ? "#dc3545" : "#4caf50";
        toast.style.visibility = "visible";
        setTimeout(() => {
          toast.style.visibility = "hidden";
        }, 3500);
      }

      

      // Buy Now button loading
      document.querySelectorAll(".btn-buy").forEach(button => {
        button.addEventListener("click", function () {
          const fileId = button.getAttribute("data-fileid");
          window.location.href = `/file/${fileId}`;
        });
      });

      // Download button loading
      document.querySelectorAll(".btn-success[download]").forEach(link => {
        link.addEventListener("click", function (e) {
          // Show spinner and disable link
          link.classList.add("loading");
          const originalHTML = link.innerHTML;
          link.innerHTML = 'Preparing <span class="btn-spinner"></span>';
          setTimeout(() => {
            link.classList.remove("loading");
            link.innerHTML = originalHTML;
          }, 1200); // Simulate loading for 1.2s
        });
      });

      // PDF Preview Modal (if you want to show PDF in modal instead of new tab)
      document.querySelectorAll(".btn-preview").forEach(btn => {
        btn.addEventListener("click", function (e) {
          e.preventDefault();
          e.stopPropagation();
          const pdfUrl = this.getAttribute("data-previewpdf");
          document.getElementById("pdfPreviewFrame").src = pdfUrl;
          const modal = new bootstrap.Modal(document.getElementById("pdfModal"));
          modal.show();
        });
      });

      // Category filter functionality
      const categoryButtons = document.querySelectorAll(".category-btn");
      categoryButtons.forEach(button => {
        button.addEventListener("click", function () {
          const category = this.getAttribute("data-category");
          // Remove active class from all buttons
          categoryButtons.forEach(btn => btn.classList.remove("active"));
          // Add active class to the clicked button
          this.classList.add("active");

          fileCards.forEach(card => {
            const cardCategory = card.getAttribute("data-category");
            if (category === "all" || cardCategory.toLocaleLowerCase() === category.toLocaleLowerCase()) {
              card.style.display = "";
            } else {
              card.style.display = "none";
            }
          });
        });
      });

      // // Notification Bell Logic
      // const notificationBellBtn = document.getElementById('notificationBellBtn');
      // const notificationBox = document.getElementById('notificationBox');
      // const notificationList = document.getElementById('notificationList');
      // const notificationDot = document.getElementById('notificationDot');

      // // Show notification box and fetch notifications
      // notificationBellBtn.addEventListener('click', function () {
      //   if (notificationBox.style.display === 'block') {
      //     notificationBox.style.display = 'none';
      //     return;
      //   }
      //   notificationBox.style.display = 'block';
      //   notificationList.innerHTML = '<div style="color:#888;">Loading...</div>';
      //   fetch('/notifications')
      //     .then(res => res.json())
      //     .then(data => {
      //       if (data.notifications && data.notifications.length > 0) {
      //         notificationList.innerHTML = data.notifications.map(n =>
      //           `<div style="padding:10px 0;border-bottom:1px solid #f3f4f6;">
      //             <span style="color:#7c3aed;font-weight:600;">${n.message}</span>
      //             <div style="font-size:0.93em;color:#888;margin-top:2px;">${new Date(n.DateTime).toLocaleString()}</div>
      //           </div>`
      //         ).join('');
      //       } else {
      //         notificationList.innerHTML = '<div style="color:#888;">No notifications.</div>';
      //       }
      //       notificationDot.style.display = 'none';
      //     })
      //     .catch(() => {
      //       notificationList.innerHTML = '<div style="color:#ef4444;">Failed to load notifications.</div>';
      //     });
      // });

      // // Close notification box when clicking outside
      // document.addEventListener('click', function (e) {
      //   if (!notificationBox.contains(e.target) && !notificationBellBtn.contains(e.target)) {
      //     notificationBox.style.display = 'none';
      //   }
      // });

      // // Close button logic
      // window.closeNotificationBox = function() {
      //   notificationBox.style.display = 'none';
      // };

      // // Optionally, show red dot if there are unseen notifications (fetch on load)
      // fetch('/notifications?unseen=1')
      //   .then(res => res.json())
      //   .then(data => {
      //     if (data.notifications && data.notifications.length > 0) {
      //       notificationDot.style.display = 'block';
      //     }
      //   });

      document.querySelectorAll('.btn-copy-link').forEach(btn => {
        btn.addEventListener('click', function () {
          let link = btn.getAttribute('data-link');
          if (link.startsWith('/')) {
            link = window.location.origin + link;
          }
          // Try Clipboard API first
          if (navigator.clipboard && window.isSecureContext) {
            navigator.clipboard.writeText(link).then(() => {
              btn.innerHTML = '<i class="fa fa-check"></i> Copied!';
              setTimeout(() => {
                btn.innerHTML = '<i class="fa fa-link"></i> Copy Link';
              }, 1200);
            });
          } else {
            // Fallback for older browsers
            const input = document.createElement('input');
            input.value = link;
            document.body.appendChild(input);
            input.select();
            input.setSelectionRange(0, 99999); // For mobile devices
            document.execCommand('copy');
            document.body.removeChild(input);
            btn.innerHTML = '<i class="fa fa-check"></i> Copied!';
            setTimeout(() => {
              btn.innerHTML = '<i class="fa fa-link"></i> Copy Link';
            }, 1200);
          }
        });
      });
    });
  </script>

  <footer style="display: flex; flex-direction: column; align-items: center; background:#f8fafc;border-top:1.5px solid #ececec;margin-bottom: 0px; margin-top:64px;box-shadow:0 -2px 8px rgba(124,58,237,0.04);">
     <span style="color:#7c3aed;font-weight:600;font-size:1.08em;">&copy; <%= new Date().getFullYear() %> PDF Store</span>
    <span style="color:#888;font-size:0.98em;margin-left:8px;">All rights reserved.</span>
    <br>
    <span style="color:#6366f1;font-size:0.98em;">
      Created by <b>Prasanna M</b> &mdash; <a href="mailto:prasannaprasanna35521@example.com" style="color:#7c3aed;text-decoration:underline;">prasannaprasanna35521@example.com</a>
    </span>
  </footer>
</body>
</html>