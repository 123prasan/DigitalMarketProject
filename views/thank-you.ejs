<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Thank You for Your Purchase | PDF Store</title>

    <meta name="robots" content="noindex, nofollow">

    <link rel="icon" href="/images/logo.png" type="image/png" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet" />
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Sora:wght@400;600;700;800&display=swap" rel="stylesheet">
    
    <style>
      /* --- Galactic Dark Theme --- */
      :root {
        --font-family: 'Sora', sans-serif;
        --color-primary: #8b5cf6;
        --color-secondary: #ec4899;
        --color-success: #22c55e;
        --color-text: #e5e7eb;
        --color-text-muted: #9ca3af;
        --color-bg-dark: #111827;
        --color-bg-card: rgba(31, 41, 55, 0.5);
        --color-border: rgba(255, 255, 255, 0.1);
        --radius-xl: 1.25rem;
        --shadow-lg: 0 10px 15px -3px rgba(0, 0, 0, 0.2), 0 4px 6px -4px rgba(0, 0, 0, 0.1);
        --transition: 0.3s ease-in-out;
        --gradient-download: linear-gradient(90deg, rgba(77, 54, 208, 1) 0%, rgba(132, 116, 254, 1) 100%);
      }

      body {
        font-family: var(--font-family);
        color: var(--color-text);
        background-color: var(--color-bg-dark);
        background-image: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" width="100" height="100" viewBox="0 0 100 100"><g fill="%231f2937"><circle cx="20" cy="20" r="1.5"/><circle cx="50" cy="50" r="1"/><circle cx="80" cy="30" r="1.2"/><circle cx="10" cy="70" r="1"/><circle cx="90" cy="80" r="1.5"/></g></svg>');
        display: flex;
        flex-direction: column;
        min-height: 100vh;
      }
      
      main {
        flex-grow: 1;
        display: flex;
        align-items: center;
        justify-content: center;
        text-align: center;
        padding: 2rem;
      }

      @keyframes fadeIn { from { opacity: 0; transform: translateY(20px); } to { opacity: 1; transform: translateY(0); } }

      .thank-you-card {
        width: 100%;
        max-width: 600px;
        background-color: var(--color-bg-card);
        backdrop-filter: blur(12px);
        -webkit-backdrop-filter: blur(12px);
        border: 1px solid var(--color-border);
        border-radius: var(--radius-xl);
        box-shadow: var(--shadow-lg);
        padding: 3rem;
        animation: fadeIn 0.6s ease-out forwards;
      }

      .thank-you-icon {
        font-size: 4rem;
        color: var(--color-success);
        margin-bottom: 1.5rem;
      }

      .thank-you-title {
        font-weight: 800;
        font-size: 2.25rem;
        color: #fff;
        margin-bottom: 0.5rem;
      }
      
      .thank-you-subtitle {
        font-size: 1.1rem;
        color: var(--color-text-muted);
        margin-bottom: 2rem;
      }
      
      .filename-highlight {
        color: var(--color-primary);
        font-weight: 700;
      }
      
      .download-section {
        margin-top: 2.5rem;
        padding-top: 2rem;
        border-top: 1px solid var(--color-border);
      }
      
      .btn-download-main {
        line-height: 1; background: var(--gradient-download); cursor: pointer;
        display: inline-flex; align-items: center; gap: 0.75em; padding: 1em 2em;
        color: #fff; border: 1px solid transparent; font-weight: 700;
        border-radius: 2em; font-size: 1.1rem;
        box-shadow: 0 0.7em 1.5em -0.5em hsla(249, 62%, 51%, 0.5);
        transition: all var(--transition);
        text-decoration: none;
      }
      .btn-download-main:hover {
        transform: translateY(-3px) scale(1.03);
        border-color: rgba(255,255,255,0.8);
        box-shadow: 0 1em 2em -0.5em hsla(249, 62%, 51%, 0.4);
      }
      .btn-download-main.disabled {
        background: #4b5563;
        cursor: not-allowed;
        transform: none;
        box-shadow: none;
        opacity: 0.5;
      }
      .btn-download-main__icon {
        width: 1.5em;
        height: 1.5em;
      }
      
      /* NEW: Styles for Countdown Timer */
      .countdown-timer {
        font-size: 2rem;
        color: var(--color-text-muted);
        margin-top: 1.5rem;
      }
      .countdown-timer #timerDisplay {
        color: var(--color-primary);
        font-weight: 700;
      }

    </style>
</head>
<body>

    <div class="page-wrapper">
        <%- include('header') %>
      
        <main>
            <div class="thank-you-card">
                <div class="thank-you-icon">
                    <i class="fas fa-check-circle"></i>
                </div>
                <h1 class="thank-you-title">Thank You!</h1>
                <p class="thank-you-subtitle">
                    Your purchase is complete. You can now download 
                    <span class="filename-highlight"><%= file.filename %></span>.
                </p>

                <div class="download-section">
                    <a  class="btn-download-main" id="downloadButton">
                        <svg class="btn-download-main__icon" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" fill="none" stroke-linecap="round" stroke-linejoin="round">
                           <path stroke="none" d="M0 0h24v24H0z" fill="none"></path>
                           <path d="M4 17v2a2 2 0 0 0 2 2h12a2 2 0 0 0 2 -2v-2"></path>
                           <path d="M7 11l5 5l5 -5"></path>
                           <path d="M12 4l0 12"></path>
                        </svg>
                        <span>Download Now</span>
                    </a>
                    
                    <p class="countdown-timer" id="countdownTimer">
                        Your secure link will expire in: <span id="timerDisplay">60</span> seconds
                    </p>
                </div>
            </div>
        </main>
      
        <%- include('footer') %>
    </div>
    
    <script>
        document.addEventListener('DOMContentLoaded', () => {
            const downloadButton = document.getElementById('downloadButton');
            const timerDisplay = document.getElementById('timerDisplay');
            const countdownContainer = document.getElementById('countdownTimer');
            downloadButton.onclick = () => {
       window.location.href="/download";
         
      // a.download = "<%=file.filename%>";
      // document.body.appendChild(a);
     
    };
            let timeLeft =<%=expiry%>; // 60 seconds

            const countdownInterval = setInterval(() => {
                timeLeft--;
                if (timerDisplay) {
                    timerDisplay.textContent = timeLeft;
                }

                if (timeLeft <= 0) {
                    clearInterval(countdownInterval);
                    
                    // Handle expiry
                    if (downloadButton) {
                        downloadButton.classList.add('disabled');
                        downloadButton.removeAttribute('href'); // Remove link to prevent clicking
                        downloadButton.innerHTML = `<span>Link Expired</span>`;
                    }
                    if (countdownContainer) {
                        countdownContainer.textContent = 'Your secure download link has expired.';
                    }
                }
            }, 1000); // Run every second
        });
    </script>

</body>
</html>