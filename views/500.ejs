<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Server Error | Vidyari</title>
    <meta name="robots" content="noindex, nofollow">
    
    <link rel="icon" href="/images/logo.png" type="image/png" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Sora:wght@400;600;700;800&display=swap" rel="stylesheet">
    
    <style>
      :root {
        --font-family: 'Sora', sans-serif;
        --color-primary: #8b5cf6;
        --color-text: #e5e7eb;
        --color-bg-dark: #111827;
        --gradient-buy: linear-gradient(90deg, var(--color-primary) 0%, #ec4899 100%);
      }
      body {
        font-family: var(--font-family);
        color: var(--color-text);
        background-color: var(--color-bg-dark);
        background-image: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" width="100" height="100" viewBox="0 0 100 100"><g fill="%231f2937"><circle cx="20" cy="20" r="1.5"/><circle cx="50"cy="50" r="1"/><circle cx="80" cy="30" r="1.2"/><circle cx="10" cy="70" r="1"/><circle cx="90" cy="80" r="1.5"/></g></svg>');
        display: flex;
        flex-direction: column;
        min-height: 100vh;
      }
      main {
        flex-grow: 1;
        display: flex;
        align-items: center;
        justify-content: center;
        text-align: center;
      }
      .error-icon {
        font-size: 5rem;
        color: var(--color-primary);
        margin-bottom: 1.5rem;
        animation: float 4s ease-in-out infinite;
      }
      @keyframes float {
        0%, 100% { transform: translateY(0); }
        50% { transform: translateY(-15px); }
      }
      .error-title {
        font-weight: 800;
        font-size: 2.5rem;
        color: #fff;
      }
      .error-message {
        font-size: 1.1rem;
        color: #9ca3af;
        max-width: 500px;
        margin: 1rem auto 2rem;
      }
      .btn-home {
        background-image: var(--gradient-buy);
        border: none;
        color: #fff;
        padding: 0.75rem 2rem;
        border-radius: 2rem;
        font-weight: 700;
        text-decoration: none;
        transition: all 0.3s ease;
      }
      .btn-home:hover {
        transform: translateY(-3px);
        box-shadow: 0 10px 20px rgba(0,0,0,0.2);
      }
    </style>
</head>
<body>
    <div class="page-wrapper">
      
      
        <main class="container">
            <div class="error-content">
                <div class="error-icon">
                    <i class="fas fa-cogs"></i>
                </div>
                <h1 class="error-title">Oops! Something went wrong.</h1>
                <p class="error-message">
                    We're sorry, but it seems there's a technical issue on our end. Our team has been automatically notified and we're working to fix it.
                </p>
                <a href="/" class="btn-home">
                    <i class="fas fa-home me-2"></i>
                    Return to Homepage
                </a>
            </div>
        </main>
      
       
    </div>
</body>
</html>