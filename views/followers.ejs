<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Followers - Vidyari</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;600;700;900&display=swap" rel="stylesheet">
<style>
    .followers-page-wrapper {
        --font-main: 'Montserrat', sans-serif;
        --color-bg: #fff;
        --color-black: #000000;
        --color-primary: #ff66a3; /* Pink */
        --color-secondary: #4ade80; /* Green */
        --color-accent: #ffd60a;   /* Yellow */
        --color-focus: #1ac2ff;   /* Blue */
        --transition-fast: 0.2s ease;
    }
    .followers-page-wrapper {
        font-family: var(--font-main);
        background-color: var(--color-focus); /* Changed background for variety */
        color: var(--color-black);
        min-height: 100vh;
        padding: 1rem;
    }
    @media (min-width: 768px) {
        .followers-page-wrapper {
            padding: 2rem;
        }
    }
    .followers-list-container {
        /* max-width: 800px; */
        margin: 0 auto;
        background: var(--color-bg);
        border: 4px solid var(--color-black);
        box-shadow: 12px 12px 0 var(--color-black);
        padding: 1.5rem;
    }
    .container-title {
        font-size: clamp(1.75rem, 5vw, 2.5rem);
        font-weight: 900;
        text-transform: uppercase;
        margin-bottom: 2rem;
        padding-bottom: 1rem;
        border-bottom: 4px solid var(--color-black);
        text-align: center;
    }
    .user-list {
        display: grid;
        gap: 1.5rem;
    }
    .user-card {
        display: flex;
        align-items: center;
        gap: 1rem;
        padding: 1rem;
        border: 3px solid var(--color-black);
        transition: all var(--transition-fast);
    }
    .user-card:hover {
        background-color: #f0f0f0;
        box-shadow: 6px 6px 0 var(--color-black);
        transform: translate(-3px, -3px);
    }
    .user-avatar {
        width: 60px;
        height: 60px;
        border-radius: 50%;
        border: 3px solid var(--color-black);
        object-fit: cover;
        flex-shrink: 0;
    }
    .user-info {
        flex-grow: 1;
        text-decoration: none;
        color: var(--color-black);
        min-width: 0;
    }
    .user-fullname {
        font-weight: 700;
        font-size: 1.1rem;
        white-space: nowrap;
        overflow: hidden;
        text-overflow: ellipsis;
    }
    .user-username {
        font-size: 0.9rem;
        color: #555;
    }
    .chunky-btn {
        border: 3px solid var(--color-black);
        box-shadow: 4px 4px 0 var(--color-black);
        font-weight: 700;
        transition: all var(--transition-fast);
        cursor: pointer;
        padding: 8px 16px;
        background-color: var(--color-bg);
        color: var(--color-black);
        text-decoration: none;
        display: inline-block;
        font-size: 0.9rem;
        white-space: nowrap;
    }
    .chunky-btn:hover {
        transform: translate(2px, 2px);
        box-shadow: 2px 2px 0 var(--color-black);
        background-color: var(--color-focus);
    }
    .chunky-btn.remove {
        background-color: var(--color-primary);
    }
    .empty-state {
        text-align: center;
        padding: 4rem 1rem;
        border: 4px dashed var(--color-black);
    }
    .empty-state i { font-size: 3rem; margin-bottom: 1rem; }
    .empty-state p { font-size: 1.2rem; font-weight: 700; }
    
    @media (max-width: 576px) {
        .user-card {
            flex-direction: column;
            align-items: flex-start;
        }
        .user-info {
            width: 100%;
        }
        .chunky-btn {
            width: 100%;
            text-align: center;
            margin-top: 0.5rem;
        }
    }
</style>
</head>
<body>
    <div class="followers-page-wrapper">
        

        <div class="followers-list-container">
            <h1 class="container-title">Followers</h1>

            <div class="user-list">
                <% if (followersList && followersList.length > 0) { %>
                    <% followersList.forEach(user => { %>
                        <div class="user-card">
                            <img src="<%= user.profilePicUrl || '/images/avatar.jpg' %>" alt="<%= user.username %>'s Avatar" class="user-avatar">
                            <a href="/profile/<%= user.username %>" class="user-info">
                                <div class="user-fullname"><%= user.fullname || user.username %></div>
                                <div class="user-username">@<%= user.username %></div>
                            </a>
                        </div>
                    <% }) %>
                <% } else { %>
                    <div class="empty-state">
                        <i class="fas fa-users"></i>
                        <p>You have no followers yet.</p>
                    </div>
                <% } %>
            </div>
        </div>
    </div>
</body>
</html>